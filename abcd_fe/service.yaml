apiVersion: v1
kind: Service
metadata:
  name: abcd-fe-service
spec:
  type: NodePort
  selector:
    app: abcd-fe
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080 # 미니쿠베 외부 접속용 포트